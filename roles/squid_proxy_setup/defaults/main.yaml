---
esp: /etc/squid/passwd

cred:
  - path: "{{ esp }}"
    name: ololosh
    password: 123456

  - path: "{{ esp }}"
    name: manolo
    password: qwerty

  - path: "{{ esp }}"
    name: odmin
    password: odmin

whitelist:
  - .google.com
  - .ubuntu.com
  - .oracle.com

htpasswd_required_packages:
  - apache2-utils
  - python3-passlib
